import { Link, Navigate, redirect, useLocation } from "react-router-dom";

const Login = () => {
  // const location = useLocation();
  // const url = location.state.url;
  // return <Link to={url} replace></Link>;
  return (
    <div>
      <h1>Hello</h1>
    </div>
  );
};

export default Login;
